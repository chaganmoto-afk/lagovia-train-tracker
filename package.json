{
  "name": "lagovia-train-tracker",
  "version": "1.0.0",
  "description": "Lagovia Train Tracker monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix api && npm install --prefix frontend",
    "dev": "concurrently \"npm start --prefix api\" \"cross-env PORT=3001 npm start --prefix frontend\"",
    "start:api": "npm start --prefix api",
    "start:frontend": "cross-env PORT=3001 npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
